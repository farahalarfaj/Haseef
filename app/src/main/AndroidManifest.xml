<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.haseef">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Haseef">
        <activity
            android:name=".tabbedRestock"
            android:label="@string/title_activity_tabbed_restock"
            android:theme="@style/Theme.Haseef.NoActionBar"></activity>
        <activity
            android:name=".tabbedproduct"
            android:label="@string/title_activity_tabbedproduct"
            android:theme="@style/Theme.Haseef.NoActionBar" />
        <activity android:name=".products" />
        <activity android:name=".statistic" />
        <activity android:name=".Restock" />
        <activity android:name=".notifications" />
        <activity android:name=".setting" />
        <activity android:name=".HomeStaff" />
        <activity android:name=".register" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>